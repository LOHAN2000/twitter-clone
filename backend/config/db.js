import mongoose from "mongoose";
import dotenv from 'dotenv'

dotenv.config()

const connectMongoDB = async () => {
  try {
    
  } catch (error) {
    console.log('Error connectino to mongoDB: ', error)
  }
}